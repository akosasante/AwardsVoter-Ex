<h1><%= @show.name %></h1>

<h3>Categories:</h3>
<ul>
  <%= for category <- @show.categories do %>
    <li>
    <strong><%= category.name %></strong>
    <span> (<%= Enum.count(category.contestants) %> nominees)</span>
    <span><%= link "Show", to: Routes.category_path(@conn, :show, category.name) %></span>
    </li>
  <% end %>
</ul>

<span><%= link "Edit", to: Routes.show_path(@conn, :edit, @show.name) %></span>
<span><%= link "Back", to: Routes.show_path(@conn, :index) %></span>
